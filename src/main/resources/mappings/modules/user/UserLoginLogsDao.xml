<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.msframe.modules.user.dao.UserLoginLogsDao">
    
	<sql id="userLoginLogsColumns">
		a.id AS "id",
		a.user_id AS "user.id",
		a.last_login_time AS "lastLoginTime",
		a.login_ip AS "loginIp",
		a.login_type AS "loginType",
		a.openid AS "openid",
		a.status AS "status",
		a.sort AS "sort",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.del_flag AS "delFlag",
		a.remarks AS "remarks",
		a.remark1 AS "remark1",
		userInfo.nickname AS "user.nickname"
	</sql>
	
	<sql id="userLoginLogsJoins">
		LEFT JOIN user userInfo ON userInfo.id = a.user_id
	</sql>
	
    
	<select id="get" resultType="UserLoginLogs" >
		SELECT 
			<include refid="userLoginLogsColumns"/>
		FROM user_login_logs a
		<include refid="userLoginLogsJoins"/>
		WHERE a.id = #{id}
	</select>
	
	<select id="findList" resultType="UserLoginLogs" >
		SELECT 
			<include refid="userLoginLogsColumns"/>
		FROM user_login_logs a
		<include refid="userLoginLogsJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
			<if test="user != null and user.id != null and user.id != ''">
				AND a.user_id = #{user.id}
			</if>
			<if test="lastLoginTime != null and lastLoginTime != ''">
				AND a.last_login_time = #{lastLoginTime}
			</if>
			<if test="loginIp != null and loginIp != ''">
				AND a.login_ip LIKE
				<if test="dbName == 'oracle'">'%'||#{loginIp}||'%'</if>
				<if test="dbName == 'mssql'">'%'+#{loginIp}+'%'</if>
				<if test="dbName == 'mysql'">concat('%',#{loginIp},'%')</if>
			</if>
		</where>
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.sort DESC
			</otherwise>
		</choose>
	</select>
	
	<select id="findAllList" resultType="UserLoginLogs" >
		SELECT 
			<include refid="userLoginLogsColumns"/>
		FROM user_login_logs a
		<include refid="userLoginLogsJoins"/>
		<where>
			a.del_flag = #{DEL_FLAG_NORMAL}
		</where>		
		<choose>
			<when test="page !=null and page.orderBy != null and page.orderBy != ''">
				ORDER BY ${page.orderBy}
			</when>
			<otherwise>
				ORDER BY a.update_date DESC
			</otherwise>
		</choose>
	</select>
	
	<insert id="insert">
		INSERT INTO user_login_logs(
			id,
			user_id,
			last_login_time,
			login_ip,
			login_type,
			openid,
			status,
			sort,
			create_by,
			create_date,
			update_by,
			update_date,
			del_flag,
			remarks,
			remark1
		) VALUES (
			#{id},
			#{user.id},
			#{lastLoginTime},
			#{loginIp},
			#{loginType},
			#{openid},
			#{status},
			#{sort},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{delFlag},
			#{remarks},
			#{remark1}
		)
	</insert>
	
	<update id="update">
		UPDATE user_login_logs SET 	
			user_id = #{user.id},
			last_login_time = #{lastLoginTime},
			login_ip = #{loginIp},
			login_type = #{loginType},
			openid = #{openid},
			status = #{status},
			sort = #{sort},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks},
			remark1 = #{remark1}
		WHERE id = #{id}
	</update>
	
	
	<!--物理删除-->
	<update id="delete">
		DELETE FROM user_login_logs
		WHERE id = #{id}
	</update>
	
	<!--逻辑删除-->
	<update id="deleteByLogic">
		UPDATE user_login_logs SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>
	
	
	<!-- 根据实体名称和字段名称和字段值获取唯一记录 -->
	<select id="findUniqueByProperty" resultType="UserLoginLogs" statementType="STATEMENT">
		select * FROM user_login_logs  where ${propertyName} = '${value}'
	</select>


	<!-- 排序功能 Begin -->
	<!-- 读取上一条数据 -->
	<select id="selectTopSort" resultType="java.lang.Long">
		SELECT sort FROM user_login_logs WHERE  del_flag = '0' and sort !=''  ORDER BY sort ASC limit 0,1
	</select>

	<select id="selectPreviousSort" resultType="java.util.Map" parameterType="java.util.Map">
		SELECT id,sort FROM user_login_logs WHERE sort &gt; #{sort} and del_flag = '0' ORDER BY sort ASC limit 0,1
	</select>

	<!-- 读取下一条数据 -->
	<select id="selectNextSort" resultType="java.util.Map" parameterType="java.util.Map">
		SELECT id,sort FROM user_login_logs WHERE sort &lt; #{sort} and del_flag = '0' ORDER BY sort DESC limit 0,1
	</select>

	<!-- 修改当前数据的排序 -->
	<update id="updateSelfSort" parameterType="java.util.Map">
		UPDATE user_login_logs SET sort = #{sort} WHERE id = #{id}
	</update>

	<!-- 修改当前的上一条、下一条的排序数据 -->
	<update id="updateOtherSort" parameterType="java.util.Map">
		UPDATE user_login_logs SET sort = #{sort} WHERE  sort = #{origianlSort}
	</update>
	<!-- 排序功能 End -->
</mapper>