<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.msframe.modules.cms.dao.CmsMovieStreamDao">

    <sql id="cmsMovieStreamColumns">
		a.id AS "id",
		a.movie_id AS "movieId.id",
		a.aspect_ratio_id AS "aspectRatioId",
		a.duration AS "duration",
		a.closed_captioning AS "closedCaptioning",
		a.resolution_id AS "resolutionId",
		a.file_size AS "fileSize",
		a.stream_id AS "streamId",
		a.content_id AS "contentId",
		a.video_type_id AS "videoTypeId",
		a.audio_type AS "audioType",
		a.system_layer_id AS "systemLayerId",
		a.copyright_id AS "copyrightId",
		a.copyright_endtime AS "copyrightEndtime",
		a.copyright_provider AS "copyrightProvider",
		a.license_number AS "licenseNumber",
		a.license AS "license",
		a.movies_playinfo AS "moviesPlayinfo",
		a.volume AS "volume",
		a.audit_number AS "auditNumber",
		a.status AS "status",
		a.sort AS "sort",
		a.create_by AS "createBy.id",
		a.create_date AS "createDate",
		a.update_by AS "updateBy.id",
		a.update_date AS "updateDate",
		a.del_flag AS "delFlag",
		a.remarks AS "remarks",
		a.remark1 AS "remark1"
	</sql>

    <sql id="cmsMovieStreamJoins">
    </sql>


    <select id="get" resultType="CmsMovieStream">
        SELECT
        <include refid="cmsMovieStreamColumns"/>
        FROM cms_movie_stream a
        <include refid="cmsMovieStreamJoins"/>
        WHERE a.id = #{id}
    </select>

    <select id="findList" resultType="CmsMovieStream">
        SELECT
        <include refid="cmsMovieStreamColumns"/>
        FROM cms_movie_stream a
        <include refid="cmsMovieStreamJoins"/>
        <where>
            a.del_flag = #{DEL_FLAG_NORMAL}
            <if test="movieId !=null and movieId !='' and movieId.id !=null and movieId.id !=''">
                AND a.movie_id = #{movieId.id}
            </if>
            <if test="resolutionId != null and resolutionId != ''">
                AND a.resolution_id = #{resolutionId}
            </if>
            <if test="volume != null and volume != ''">
                AND a.volume = #{volume}
            </if>
            <if test="auditNumber != null and auditNumber != ''">
                AND a.audit_number = #{auditNumber}
            </if>
            <if test="status != null and status != '' or status == 0">
                AND a.status = #{status}
            </if>
        </where>
        <choose>
            <when test="page !=null and page.orderBy != null and page.orderBy != ''">
                ORDER BY ${page.orderBy}
            </when>
            <otherwise>
                ORDER BY a.update_date DESC
            </otherwise>
        </choose>
    </select>

    <select id="findAllList" resultType="CmsMovieStream">
        SELECT
        <include refid="cmsMovieStreamColumns"/>
        FROM cms_movie_stream a
        <include refid="cmsMovieStreamJoins"/>
        <where>
            a.del_flag = #{DEL_FLAG_NORMAL}
        </where>
        <choose>
            <when test="page !=null and page.orderBy != null and page.orderBy != ''">
                ORDER BY ${page.orderBy}
            </when>
            <otherwise>
                ORDER BY a.update_date DESC
            </otherwise>
        </choose>
    </select>

    <insert id="insert">
		INSERT INTO cms_movie_stream(
			id,
			movie_id,
			aspect_ratio_id,
			duration,
			closed_captioning,
			resolution_id,
			file_size,
			stream_id,
			content_id,
			video_type_id,
			audio_type,
			system_layer_id,
			copyright_id,
			copyright_endtime,
			copyright_provider,
			license_number,
			license,
			movies_playinfo,
			volume,
			audit_number,
			status,
			sort,
			create_by,
			create_date,
			update_by,
			update_date,
			del_flag,
			remarks,
			remark1
		) VALUES (
			#{id},
			#{movieId.id},
			#{aspectRatioId},
			#{duration},
			#{closedCaptioning},
			#{resolutionId},
			#{fileSize},
			#{streamId},
			#{contentId},
			#{videoTypeId},
			#{audioType},
			#{systemLayerId},
			#{copyrightId},
			#{copyrightEndtime},
			#{copyrightProvider},
			#{licenseNumber},
			#{license},
			#{moviesPlayinfo},
			#{volume},
			#{auditNumber},
			#{status},
			#{sort},
			#{createBy.id},
			#{createDate},
			#{updateBy.id},
			#{updateDate},
			#{delFlag},
			#{remarks},
			#{remark1}
		)
	</insert>

    <update id="update">
		UPDATE cms_movie_stream SET 	
			movie_id = #{movieId.id},
			aspect_ratio_id = #{aspectRatioId},
			duration = #{duration},
			closed_captioning = #{closedCaptioning},
			resolution_id = #{resolutionId},
			file_size = #{fileSize},
			stream_id = #{streamId},
			content_id = #{contentId},
			video_type_id = #{videoTypeId},
			audio_type = #{audioType},
			system_layer_id = #{systemLayerId},
			copyright_id = #{copyrightId},
			copyright_endtime = #{copyrightEndtime},
			copyright_provider = #{copyrightProvider},
			license_number = #{licenseNumber},
			license = #{license},
			movies_playinfo = #{moviesPlayinfo},
			volume = #{volume},
			audit_number = #{auditNumber},
			status = #{status},
			sort = #{sort},
			update_by = #{updateBy.id},
			update_date = #{updateDate},
			remarks = #{remarks},
			remark1 = #{remark1}
		WHERE id = #{id}
	</update>


    <!--物理删除-->
    <update id="delete">
		DELETE FROM cms_movie_stream
		WHERE id = #{id}
	</update>

    <!--逻辑删除-->
    <update id="deleteByLogic">
		UPDATE cms_movie_stream SET 
			del_flag = #{DEL_FLAG_DELETE}
		WHERE id = #{id}
	</update>


    <!-- 根据实体名称和字段名称和字段值获取唯一记录 -->
    <select id="findUniqueByProperty" resultType="CmsMovieStream" statementType="STATEMENT">
		select * FROM cms_movie_stream  where ${propertyName} = '${value}'
	</select>

</mapper>